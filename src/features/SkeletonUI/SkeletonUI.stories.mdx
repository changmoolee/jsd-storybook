import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import SkeletonUI from "./SkeletonUI";

<Meta
  title="Feature/SkeletonUI"
  component={SkeletonUI}
  argTypes={{
    width: {
      name: "width",
      type: { required: false },
      description:
        "Handling the size of the element that will be rendered by SkeletonUI. ('px' and '%' units are ok.)",
      defaultValue: "100%",
      table: {
        type: { summary: "string" },
      },
    },
    children: {
      name: "children",
      description: "Pass in the children that will be rendered by SkeletonUI",
      table: {
        type: { summary: "node" },
      },
      control: {
        type: null,
      },
    },
  }}
/>

# SkeletonUI

## Overview

export const Template = (args) => <SkeletonUI {...args} />;

<Canvas>
  <Story
    name="Rectangle"
    args={{ children: <div style={{ width: "200px", height: "50px" }} /> }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Circle"
    args={{
      children: (
        <div style={{ width: "50px", height: "50px", borderRadius: "50%" }} />
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Text"
    args={{ children: <div style={{ width: "100%", height: "16px" }} /> }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Component Api

<ArgsTable story="Rectangle" />
